# Filenames
TIMEINFO_FILENAME = "timeinfo.json"
IMAGE_FILE_FORMATSTRING = "{id}-{index}.{format}"
IMAGE_FILE_PART_FORMATSTRING = "{id}_part{part}-{index}.{format}"

# Render & Scene values
VIEWPORT_CLIP_NEAR = 0
VIEWPORT_CLIP_FAR = 40

# Split Lidar 360° panorama into multiple outputs
PANORAMA_SENSOR_PARTITION_AMOUNT = 4